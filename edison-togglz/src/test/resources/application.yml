spring:
    main:
        allow-bean-definition-overriding: true
    application:
        name: togglztest

server:
    servlet:
        context-path: /togglztest
    port: 8085

management:
    endpoints:
        web:
            exposure:
                include: '*'
    endpoint:
        loggers:
            enabled: true

edison:
    togglz:
        mongo:
            enabled: false

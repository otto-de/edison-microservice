
dependencies {
    compile libs.springBoot
    compile libs.testUnit
    compile libs.togglz
    compile libs.testTogglz
}

artifacts {
    archives jar
    archives javadocJar
    archives sourcesJar
}

signing {
    sign configurations.archives
}

